global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'fastapi_backend'
    static_configs:
      - targets: [ 'backend:8000' ]
        labels:
          app: 'fastapi'
          application: 'fastapi'
          app_name: 'fastapi'